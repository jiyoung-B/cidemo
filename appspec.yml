version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/cidemo
    overwrite: yes

permissions:
  - object: /home/ubuntu/cidemo
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755

hooks:
  ApplicationStart:
    - location: scripts/deploy.sh
      timeout: 120
      runas: ubuntu
